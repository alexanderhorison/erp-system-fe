version: '3.8'
services:
  fe-erp:
    image: ${CONTAINER_NAME}:latest
    container_name: ${CONTAINER_NAME}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT}:${PORT}"
    environment:
      - NODE_ENV=production
      - PORT=${PORT}
    restart: unless-stopped
    networks:
      - tunnel

networks:
  tunnel:
    external: true
